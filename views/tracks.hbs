
<div id="player">

    {{#each playList}}
        {{#each this}}

            {{log this.this}}
                <div class="player-line">
                    <h2>{{this.name}}</h2>
                    <audio controls src="{{this.preview_url}}"></audio>
                </div>
            
        {{/each}}
    {{/each}}

</div>
